export interface Point {
  x: number;
  y: number;
}

export const width = 500;
export const height = 500;

export const center: Point = {
  x: width / 2,
  y: width / 2,
};

export const armSize = {
  width: width / 4,
  height: 20,
};
